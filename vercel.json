{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx react-router build",
  "installCommand": "npm install",
  "framework": "react-router",
  "outputDirectory": "build"
}
